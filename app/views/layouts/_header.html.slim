<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="navbar-brand">
    = link_to '本棚アプリ', posts_path, class: "white"
  </div>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarColor01">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
      - if user_signed_in?
        .nav-link
          = link_to '投稿', posts_path, class: "white"
      </li>
      <li class="nav-item">
      - if user_signed_in?
        .nav-link
          = link_to '本棚', libraries_path, class: "white"
      </li>
      <li class="nav-item">
      - if user_signed_in?
        .nav-link
          = link_to '本', books_path, class: "white"
      </li>
    </ul>
    - if user_signed_in?
      = link_to "log_out", destroy_user_session_path, method: :delete, class: "white"
    - else
      = link_to "log_in", new_user_session_path, class: "white"
  </div>
</nav>
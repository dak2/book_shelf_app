- flash.each do |type, msg|
  - color = 'light-blue lighten-3' if type == 'notice'
  - color = 'red lighten-1' if type == 'alert'

  = javascript_tag "M.toast({html: '#{msg}', classes: '#{color}', displayLength: 4000})"